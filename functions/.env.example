# Local emulator testing only — production values are set via
# `firebase functions:secrets:set <NAME>`, never committed here.
# Copy this to functions/.env.local (gitignored) for local emulator use.

GOOGLE_WEB_CLIENT_ID=
GOOGLE_WEB_CLIENT_SECRET=
ALLOWED_EMAILS=
